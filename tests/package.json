{
    "scripts": {
        "test": "node esm.mjs && node cjs.js && esr full.ts && open http://localhost:8000 && python3 -m http.server"
    },
    "devDependencies": {
        "@types/node": "^17.0.31",
        "esbuild-runner": "^2.2.1"
    },
    "dependencies": {
        "datums": "link:.."
    }
}
